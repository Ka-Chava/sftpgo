# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.
#
# Get this connection string from:
#   DO Control Panel → Databases → your cluster → Connection Details → Connection string
# The string will look like: postgres://user:password@host:25060/defaultdb?sslmode=require

SFTPGO_DATA_PROVIDER__CONNECTION_STRING=postgres://user:password@host:25060/defaultdb?sslmode=require
