# Caddy reverse proxy for SFTPGo web admin / API
# Caddy automatically obtains and renews a TLS cert via Let's Encrypt.
# Cloudflare proxies HTTPS to this Droplet; Caddy forwards to SFTPGo on the internal Docker network.

sftp.kachava.com {
    reverse_proxy sftpgo:8080
}
